var velocidade = 60

console.log(`A velocidade do seu carro é ${velocidade}km/h`)
if(velocidade > 60) { // Condição simples!
    console.log(`Vc passou da velocidade permitida...`)
}
console.log(`Dirija sempre com cinto de segurança!`)

